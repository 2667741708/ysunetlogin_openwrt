# 更新说明

按更新顺序倒序排列，最近的源码更新在最上方。历史发布内容保留在同一文件中。

## 2026-09-15 — 借网残留规则说明与射手座链式连接验证

- 查明 D321 的强制中转来自原 SSH 代理恢复脚本；Clash 刷新配置会重新注入，不是每次 WireGuard 借网必然生成。已修复生成脚本，说明默认借网、共享代理和旧 SSH 中转的区别。
- 本机进行移动 → 校园网 0 → 移动对照：射手座原配置、固定 IPv4、quic 中继模式、cubic/超时调整均在移动成功、校园网 0 连续两轮失败；TCP/443 对照节点可用。
- 验证“校园网 0 → 香江-F TCP/443 → 射手座 TUIC”可用。实际网页复测中，校园网 0 下两轮 GitHub、百度均 HTTP 200 且 curl 退出 0；记录此前前置节点超时的边界。
- 增补 [代理诊断与配置方法](docs/CAMPUS_PROXY_DIAGNOSIS.md)。没有改动运营商选择逻辑；测试后恢复移动，日常 Clash 配置保持原样。D321 原借网服务仍为运行、自动启动。

## 2026-09-15 — 校园有线认证检查修复

- D321 停用借网后，DNS 和认证 HTTPS 均可达，但原直连检查超时。
- 实机对照发现校园 HTTP 网关不回应 Python 默认 User-Agent，使用浏览器标识则返回认证页面；页面通过 `top.self.location.href` 跳转，而非 HTTP 302。
- 检查流程改用浏览器标识，识别页面中的校园 HTTPS 跳转，限制跟随次数并核对会话出口 IP；不执行 JavaScript，也不跟随页面脚本指向的外部地址。
- 修复后，D321 的独立有线网卡通过直连检查，成功登录并核实校园网 0。
- 代理排查发现 D321 置顶的强制中转规则指向未监听的本机端口。备份并移除该规则、修正配置生成脚本后，D321 在不借网的校园网 0 下连续两轮经代理访问 GitHub、百度均返回 HTTP 200。
- TCP/443 节点可用，所测 TUIC 节点仍失败；不能把该节点链路失败解释为校园网 0 无法使用所有代理。测试结束已注销临时账号并恢复原借网配置。详见 [完整代理诊断](docs/CAMPUS_PROXY_DIAGNOSIS.md)。
- 88 项测试通过，新增覆盖脚本跳转、非校园地址拒绝、错误出口 IP、循环跳转与原有直接会话。

## 2026-09-15 — 命令文档与在线设备详情

### 文档

- 新增 [命令大全](命令大全.md)，列出 netlogin.py 所有命令、别名、JSON 输入、退出码和配置方法。
- 将各版本 RELEASE_NOTES 和原 CHANGELOG 合并到本文件，按最新更新在前的顺序维护。
- 原有安装和配置文档统一链接到本文件。

### 账号页

- 在线设备改为表格，分列显示 IP、主机名、实际运营商、设备名称、类型、在线时长和上线时间。
- 保留横向、纵向滚动；双击或按 Enter 打开完整详情，支持复制全部信息。
- 主机名与设备名称分开显示；仅在本机 IP 已核对时补齐查询机器主机名。
- 运营商使用学校接口实际值；当前出口可用认证会话补齐。其他设备缺失时显示未知，不套用默认账号配置。
- 修复账号带运营商后缀时，当前设备的服务信息无法补齐的问题。

### 校园网 0 与代理的本机对照

在同一台电脑、同一 Wi-Fi 和同一代理配置下执行“移动 → 校园网 0 → 移动”，
第二轮在切换后等待 8 秒再测，并在校园网 0 下重复测试。

| 请求 | 移动（切换前） | 校园网 0（两次） | 移动（恢复后） |
| --- | --- | --- | --- |
| 百度 HTTPS，IPv4 直连 | HTTP 200 | HTTP 200 | HTTP 200 |
| 百度 HTTPS，IPv6 直连 | HTTP 200 | HTTP 200 | HTTP 200 |
| 百度 HTTPS，经本机代理 | HTTP 200 | HTTP 200 | HTTP 200 |
| GitHub HTTPS，IPv4 直连 | HTTP 200 | HTTP 200 | HTTP 200 |
| GitHub HTTPS，经所选代理节点 | HTTP 200 | 连接超时（约 8 秒） | HTTP 200 |

代理为规则模式，TUN 关闭。控制接口确认 GitHub 请求走所选 TUIC 节点。
这说明故障集中在校园网 0 到该代理节点的连接路径，不能解释为“校园网 0 完全不能上网”
或“netlogin 关闭了系统代理”。TUIC 使用 QUIC / UDP，服务出口、UDP/IPv6 路径和旧会话恢复
都是待进一步区分的原因；本次没有抓包或学校侧策略证据，不能断言学校统一封禁 UDP。
第一轮恢复移动后的短超时请求仍失败，稍后重试恢复，说明切换后还可能有节点重连等待。

使用建议：需要该节点时保持移动；校园网 0 下可另行测试 TCP / 443 类型节点。
本次未修改全局代理配置，也未替换用户选择的节点，测试完成后恢复移动。
协议与控制接口参考 [Mihomo TUIC](https://wiki.metacubex.one/config/proxies/tuic/)
和 [Mihomo API](https://wiki.metacubex.one/en/api/)。

### 其他服务器的验证范围

- 教学区有线服务器使用自身的代理，移动状态下百度、GitHub 的直连与代理请求均曾返回 HTTP 200。
- 两次尝试切换校园网 0 均未通过实际在线服务核验：第一次出现网络不可达；第二次将测试认证请求绑定物理网卡后，仍未核实校园网 0 在线。因此没有有效的远端 0/移动对照，不据此推断校园网 0 是否限制代理。
- 第二次恢复移动后，GitHub / Google 代理请求仍出现 TLS 错误。随后注销旧会话、等待 15 秒再恢复同一移动账号，并再等待 15 秒，代理访问 GitHub / 百度返回 HTTP 200，Google 返回 HTTP 302。最终已恢复移动及代理可用状态；这说明切换后的会话释放和重连也会干扰测试，不能仅凭一次失败归因于运营商限制。
- 该服务器开启 TUN，而本次发布的自动网卡绑定仅针对 Windows。Linux 上的 `ProxyHandler({})` 仍不能绕过系统层 TUN 路由；临时绑定只用于本轮排查，没有安装成服务器常驻程序。
- D408 的默认认证出口经过共享网关，不能把网关会话当作 D408 的独立运营商；D437 出现外网证书验证错误；另一台候选服务器的 SSH 连接超时。这些结果均未纳入运营商对照。

### 验证

- 82 项单元测试通过，覆盖运营商匹配、网卡直连、设备身份补齐和长字段展示。
- 真实 Tk 窗口检查通过：表格双向滚动、选择设备、完整详情和长主机名/IP。
- 更新后的 Windows 打包脚本通过 PowerShell 语法检查；本次更新源码与文档，尚未发布新版 EXE。

## 2026-09-15 — 指定运营商、校园网卡直连与 Wi-Fi 扫描

### 修复

- 指定移动、联通、电信时，只选择唯一匹配的服务；找不到或匹配不明确时停止，
  展示服务器可用服务，不再静默选择校园网或列表第一项。
- 登录成功必须核实在线账号和实际运营商，接口未返回服务或服务不一致时不报告成功。
- 修复外网可达时跳过校园认证的判断；校园认证状态优先于普通外网探测结果。
- 优先直连 `auth1.ysu.edu.cn` 获取校园会话，减少对公共网站重定向探测的依赖。
- 兼容 `--status`、`--current-status`；帮助命令不再执行空账号登录，登录失败返回非零退出码。

### 新增

- Windows 物理网卡检测：分别检查有线与无线，经验证可直连校园认证后选择出口；
  多个出口通过时优先有线，全部失败时停止。
- DNS、HTTP、HTTPS 请求绑定选定网卡及其本机 IPv4；DNS 使用该网卡的服务器，
  不回退到系统 DNS 或未经验证的默认出口，拒绝代理虚拟 IPv4 地址。
- `wifi-scan` 主动扫描并等待完成通知，显示实际可见 Wi-Fi、信号和连接状态。
- `campus-check` 检查附近 `iYanDa`、物理网卡和校园认证直连；支持 JSON 输出。
- `campus_network.example.json` 提供扫描开关、目标 SSID、网卡选择和超时配置。
- 桌面端构建脚本包含新增的网卡检测资源；旧 EXE 不会自动获得源码更新。

### 升级与使用

Windows 升级需一起保留 `netlogin.py`、`campus_network.py`、`campus_adapters.ps1`
和 `wifi_scan.py`。从仓库下载 ZIP 并完整解压即可，不能只替换单个脚本。

```text
python netlogin.py campus-check
python netlogin.py wifi-scan
python netlogin.py current-status
python netlogin.py "学号" "密码" 1
```

详细命令、配置字段与故障处理见 [校园网直连使用说明](CAMPUS_DIRECT_SETUP.md)。
本节记录源码更新；未创建新的 GitHub Release，也未发布新版 EXE。

### 验证与范围

- 74 项自动测试通过，覆盖运营商选择、代理绕过、DNS 校验、物理网卡绑定与出口选择。
- 本机 `iYanDa` 在系统代理开启时通过校园直连检测，并核实中国移动在线。
- 测试进程设置不可用代理、禁用系统 DNS 查询后，仍通过物理网卡查询到中国移动在线。
- 有线网卡未接线，只完成模拟测试；TUN 过滤模式尚未完成实网验证。
- 不自动切换 Wi-Fi，不修改系统代理、DNS 或路由表。扫描失败不会阻止正常有线检查。
- Linux / OpenWrt 保留原有直连认证路径，不启用 Windows 网卡绑定模块。

## 2.1.4

### 代理环境下的校园网直连

- 校园网探测、CAS 和 portal 请求显式绕过 Windows 系统代理、PAC 与代理环境变量。
- 资格检查会说明是否检测到代理以及程序是否已启用直连绕过。
- TUN 或透明代理仍需在代理软件中为校园网域名、`10.11.0.1` 和校园网段配置直连规则。

### 手动连接自动切换会话

- “使用此账号连接本机”和远端一键连接现在会核实账号及运营商，而不是把“外网可达但账号未知”当作成功。
- 当前账号或运营商不一致、无法确认时，会自动下线旧校园网会话并登录所选配置。
- 无需再先手动点击“下线本机”。
- 无人值守心跳继续保留保守判断，避免门户临时不可识别时误踢健康连接。

### 验证

- 增加系统代理绕过、Cookie 会话代理绕过、旧会话自动切换和未知会话强制认证测试。
- 完整发布流程包括自动测试、安全审计、EXE 自检和 ZIP 校验。

## 2.1.3

### 查询机器资格预检

- 在本机和 SSH 目标上检查校园网查询环境。
- “校园网账号”页新增“检查资格”按钮。
- 查询、设备下线或让目标机器登录运营商之前，确认目标机器能够访问校园网认证环境并取得 auth1 `sessionId`。
- 不满足时停止后续操作并提示“需要先实现访问校园局域网”。
- 预检不要求查询机器当前在线账号与所选账号相同。

### 验证

- 自动测试覆盖预检通过、预检失败、阻止登录和远端内置脚本路径。
- 发布流程继续执行安全审计、EXE 自检和发布包校验。

## 2.1.2

### 修复与改进

- “校园网账号”新增“在线设备查询位置”，默认优先选择 `c201-4090`，并允许修改为本机或其他 SSH 目标。
- 在线设备查询和设备下线操作可在远程服务器上执行，再把结构化结果返回本机界面。
- 修复账号已经在 4090 在线、但查询错误地在本机执行而显示“未检测到 auth1 新认证页面”的问题。
- SSH 远端自动探测 `python3`、`python` 和 `python.exe`，兼容 Linux 与 Windows 服务器。
- 增加 Windows CNG AES 后端；远端 Windows Python 即使没有 PyCryptodome、cryptography 或 OpenSSL，也能完成校园网密码加密。

### 实机验证

- Titan：停止 `fleet-titan` 借网后，使用“博士移动”登录中国移动成功；核对账号、运营商、物理 IP 与公网均成功；随后注销并恢复借网。
- Titan 恢复后：`fleet-titan` 为运行中、自动启动，公网探测返回 HTTP 204。
- 4090：远程查询账号 `202311030027` 成功，复用当前会话并返回设备、IP 与中国移动运营商。
- 23 项自动化测试全部通过。

## 2.1.1

这是针对本机与远程服务器连接失败的兼容性修复版本。

### 修复

- 4090、5080 预置服务器不再依赖服务器上的固定 `netlogin.py` 路径，默认使用安装包内置程序。
- 自动迁移 2.1.0 的旧预置路径和旧主机名限制；用户自己填写的自定义路径仍会保留。
- 自定义远程脚本被移动或删除时，程序会识别 Python 的“文件不存在”错误并安全回退到内置程序。
- 本机网络状态与指定账号连接继续使用同一套登录核心，当前连接的运营商会明确显示。

### 验证

- 21 项自动化测试全部通过。
- Windows 打包版内置资源、自检和安全审计通过。
- 本机在线状态实测为中国移动，指定账号连接验证成功。

### 说明

- 远程服务器通过本机 Windows OpenSSH 配置中的 Host 别名连接，不读取、不保存、不打包 SSH 私钥。
- 升级后心跳开关保留原设置。需要自动自愈时，请在“网络自愈心跳”页面为各目标启用心跳。

## 2.1.0

### 新增：本机 Windows 联网

- 内置固定的“本机 Windows”目标，不需要 SSH 或远端服务器。
- 可直接检查当前电脑的外网、校园网认证、账号、运营商与 IP 状态。
- 可选择已加密保存的任意账号和运营商，让当前电脑直接连接校园网。
- 如果当前在线账号或运营商与所选配置不一致，会先下线旧会话，再使用指定配置连接。
- “本机 Windows”也可以绑定默认账号并加入自动自愈心跳。
- 本机与远端操作使用统一串行锁，避免手动登录与心跳同时修改同一目标。
- 修复本机已在线时“查询在线设备”因 CAS 重复登录返回 HTTP 401、界面显示“操作失败：None”的问题；现在会复用匹配账号的本机认证会话，并明确显示学校接口实际返回的设备数量。

### 保持不变

- 远端 SSH 登录、账号设备管理和内置 VNC 功能继续可用。
- 账号密码继续使用当前 Windows 用户的 DPAPI 加密。
- SSH 私钥不会被读取、保存或打包。

推荐先在“一键连接”页选择“本机 Windows”，执行一次“检查本机网络状态”；确认正常后，再选择账号和运营商进行连接或启用本机自愈心跳。

## 2.0.0

这是首个面向 Windows 10/11 x64 的账号感知网络自愈版本。

### 主要功能

- 将桌面管理器、`netlogin.py`、心跳引擎和 TigerVNC Viewer 打包为单一 EXE。
- 为每台 SSH 主机绑定默认校园网账号，并独立设置检查间隔和连续异常阈值。
- 检测到掉线后自动按账号重连，并在重连后再次验证。
- 账号或运营商不匹配时纠正会话；外网正常但门户状态未知时保持现状，减少误操作。
- 失败后指数退避，避免网络或认证服务异常时形成请求风暴。
- 支持当前 Windows 用户登录后最小化自启动。
- 账号密码继续使用 Windows DPAPI 加密；SSH 私钥不读取、不保存、不打包。
- 发布包包含中文本机配置教程和 SHA256 校验文件。

### 推荐配置

- 检查间隔：60 秒。
- 连续异常阈值：2 次。
- 先完成一次“测试 SSH”和“立即检查一次”，再启用 Windows 登录自启动。

完整步骤见压缩包内的 `WINDOWS_LOCAL_SETUP.md`。
