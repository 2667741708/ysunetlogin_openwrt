# netlogin.py 命令大全

在脚本所在目录打开终端。Windows 请完整保留 `netlogin.py`、`campus_network.py`、
`campus_adapters.ps1` 和 `wifi_scan.py`；自助中心新命令还需要同目录的 `self_service.py`（Python 3）。文件名要写 `.py`。
以下账号、密码均为占位文字，不要原样用于登录。

## 1. 常用命令速查

| 命令 | 作用 | 会修改连接吗？ |
| --- | --- | --- |
| `python netlogin.py` | 显示用法 | 否 |
| `python netlogin.py --help` 或 `-h` | 显示帮助 | 否 |
| `python netlogin.py wifi-scan` | 主动扫描附近 Wi-Fi，显示名称、信号、连接状态 | 否，不会自动连接 |
| `python netlogin.py campus-check` | 检查物理有线、无线网卡及校园认证直连，默认同时扫描 Wi-Fi | 否 |
| `python netlogin.py current-status` | 查询当前出口的账号、实际服务、IP、位置和在线设备 | 否 |
| `python netlogin.py query-machine-status` | 检查本机是否有校园查询环境和 auth1 会话，输出 JSON | 否 |
| `python netlogin.py "学号" "密码" 1` | 使用指定账号登录移动 | 是 |
| `python netlogin.py "学号" "密码"` | 省略运营商编号，默认登录校园网 0 | 是 |
| `python netlogin.py logout` | 注销当前出口的校园认证会话 | 是；不会断开 Wi-Fi 本身 |
| `python netlogin.py account-status "学号" "密码"` | 查询指定账号的在线设备 | 只建立查询会话，不执行运营商登录或踢设备 |
| `python netlogin.py account-menu` | 输入账号密码后查看流量，交互选择校园网/移动/联通/电信 | 只有选择 0–3 时才登录或切换 |
| `python netlogin.py account-traffic "学号"` | 查询学校套餐总额、剩余额度、计费周期和本机实际服务 | 否；省略密码时交互输入 |
| `python netlogin.py account-login "学号" --service 1` | 确保本机使用该账号和移动，成功后查询剩余流量 | 是；核验指定服务，不回退 0 |
| `python netlogin.py account-devices "学号"` | 自助中心在线/离线登记设备、无感状态、实际服务与缺省服务 | 否；交互输入密码 |
| `python netlogin.py account-devices "学号" --service 1` | 逐台核对是否在线且实际使用移动，未知单独标记 | 否；不是切换运营商 |
| `python netlogin.py account-history "学号"` | 历史上网记录、当次服务、登录/登出时间、下线原因 | 否 |
| `python netlogin.py nosense-config "学号"` | 查询学校规定的无感有效期与单位 | 否 |
| `python netlogin.py device-offline "学号" --uuid "在线会话UUID"` | 下线指定设备；多个 `--uuid` 批量操作，`--all` 全部在线设备 | 是；加 `--dry-run` 只预览 |
| `python netlogin.py nosense-enable "学号" --mac "设备MAC"` | 为本账号已有在线设备开启无感 | 是；加 `--dry-run` 只预览 |
| `python netlogin.py nosense-register "学号" --mac "设备MAC" --expire 1` | 按 MAC 手工登记无感，期限单位取自学校配置 | 是；加 `--dry-run` 只预览 |
| `python netlogin.py nosense-disable "学号" --uuid "无感UUID"` | 关闭无感绑定；可重复 UUID，`--all` 包含离线登记设备 | 是；加 `--dry-run` 只预览 |
| `python netlogin.py login-stdin` | 从标准输入读取 JSON，连接指定账号与运营商 | 是；会切换不匹配的旧会话 |
| `python netlogin.py account-status-stdin` | 从标准输入读取账号 JSON，查询设备 | 只读查询 |
| `python netlogin.py account-offline-devices-stdin` | 从标准输入读取 JSON，下线指定账号的设备 | 是，可能下线多台设备 |

## 2. 运营商编号与切换

### 交互登录与流量查询

```powershell
python netlogin.py account-menu
```

按提示输入自己的校园账号、密码。密码交互输入不回显，不会自动保存。
程序先建立账号查询会话并显示学校套餐额度；然后选择：

```text
0 校园网 | 1 中国移动 | 2 中国联通 | 3 中国电信 | t 查询流量 | q 退出
```

- 选择 0–3 才会登录指定服务；如果本机已使用其他账号/服务，会先结束本机旧会话。
- 成功必须核实实际账号和运营商；失败不会自动尝试校园网 0 或其他运营商。
- 不选择、输入无效内容或退出菜单不会触发默认登录；退出保留当前连接。
- 登录后自动重新查询流量；即使流量接口失败，也会单独显示已经核实的登录结果。
- 这是本机当前校园出口的服务切换，不会批量下线账号下的其他设备。

只查额度，或直接指定服务：

```powershell
python netlogin.py account-traffic "学号"
python netlogin.py account-traffic "学号" --json
python netlogin.py account-login "学号" --service 0
python netlogin.py account-login "学号" --service 1 --json
python netlogin.py account-menu --accounts-file "D:\Netlogin\accounts.private.json" --account-name "我的账号"
```

三条新命令均支持账号文件；`account-traffic` 和 `account-login` 也支持 `--stdin` 读取账号密码 JSON。
`account-menu` 是交互菜单，不支持 `--stdin` / `--json`。
`account-login` 必须明确指定 `--service`，不从账号配置名推断运营商。

### 60GB 套餐的剩余流量

`account-traffic` 读取学校自助中心的真实套餐与剩余额度，不将所有账号固定为 60GB。
主要字段：

| JSON 字段 | 含义 |
| --- | --- |
| `packageName` / `period` | 学校套餐名称、本计费周期 |
| `items[].name` / `scope` | 流量类别及学校返回的适用范围 |
| `items[].totalText` / `remainingText` | 学校原始总额和剩余描述 |
| `items[].totalGBApprox` / `remainingGBApprox` / `usedGBApprox` | 换算为 GB 的近似值；未知为 `null` |
| `currentSession` | 本机现在的实际账号、服务、在线状态，以及是否属于所查账号 |
| `queriedAt` | 本次查询时间（UTC）；不是学校计费结算时间 |
| `operatorTraffic` | 运营商独立套餐额度是否可得；当前接口未提供 |

例如学校返回 `53 GB 862.62 MB`，程序会保留原文，并显示约 `53.84 GB`。
近似换算采用 1 GB = 1024 MB，学校原始描述为准；不会只取整数 53，也不会根据百分比反算精确余额。
若学校配置了多项/分区额度，会分别列出，不能随意累加；空值、未提供、无限流量文本不显示成 0 GB。

**学校账号的 60GB 套餐与移动/联通/电信独立套餐不是同一个查询结果。**
当前已验证的接口提供学校套餐额度，没有给出三个运营商各自套餐的剩余 GB。
即使本机正在使用移动，也不能把学校套餐剩余数当作移动独立套餐余量。
计费有更新延迟，切换运营商不会重置学校套餐额度。

### 服务编号

| 编号 | 服务 |
| --- | --- |
| `0` | 校园网 |
| `1` | 中国移动 |
| `2` | 中国联通 |
| `3` | 中国电信 |

指定 `1/2/3` 后，匹配失败会报错，不会自动改选校园网。
认证成功后还会核对实际账号和服务；“外网可达”不等于“运营商登录成功”。

位置参数登录如果发现当前账号或服务不符，需要先注销再登录：

```powershell
python netlogin.py current-status
python netlogin.py logout
python netlogin.py "学号" "密码" 1
python netlogin.py current-status
```

`login-stdin` 与桌面端的显式连接可自动切换旧会话。
校园网 0 的外网可达范围由学校授权决定；代理软件也需要先能连接代理节点。
选择 0 不会关闭系统代理开关，详见 [更新说明](RELEASE_NOTES.md)。

## 3. 状态查询别名与 JSON 输出

下列状态命令等价：

```powershell
python netlogin.py current-status
python netlogin.py status
python netlogin.py --status
python netlogin.py --current-status
```

以下命令可以追加 `--json`，方便程序读取完整结果：

```powershell
python netlogin.py current-status --json
python netlogin.py campus-check --json
python netlogin.py wifi-scan --json
python netlogin.py account-status "学号" "密码" --json
```

`query-machine-status` 和三个 `*-stdin` 命令直接输出 JSON，不需要额外的 `--json`。
`current-status` 查询的是执行脚本的机器当前出口；查询其他账号使用 `account-status`。

## 4. 从文件选择账号查询设备

```powershell
python netlogin.py account-status --accounts-file "D:\Netlogin\accounts.private.json" --account-name "我的移动账号" --json
```

参数别名：`--account-file` 等于 `--accounts-file`；`--name` 等于 `--account-name`。
也支持将配置名称作为位置参数：

```powershell
python netlogin.py account-status --accounts-file "D:\Netlogin\accounts.private.json" "我的移动账号"
```

账号文件示例（含密码，仅在本机保管，勿提交 Git）：

```json
{
  "accounts": [
    {"name": "我的移动账号", "username": "学号", "password": "密码"}
  ]
}
```

账号设备的 `hostname` 与 `service` 是实际返回或经核对补齐的信息。
`deviceName` 是设备名称，不一定是操作系统主机名。其他设备缺失的字段会标为未知。

### 4.1 明确查询在线状态和指定服务

```powershell
python netlogin.py account-devices "学号" --service 1 --json
python netlogin.py account-devices --accounts-file "D:\Netlogin\accounts.private.json" --account-name "我的移动账号" --service 1 --json
```

新命令省略密码会交互输入，也接受第二个位置参数密码。均支持 `--stdin` 读取
`{"username":"学号","password":"密码"}`，以及 `--accounts-file` / `--account-name`。
`--json` 输出完整结果；每个命令的详细参数可用 `--help` 查看。

- `online: true` / `state: online`：学校在线设备列表中存在此会话。
  这是认证系统报告的在线状态，不保证电脑此刻正在传输流量或一定响应探测。
- `online: false` / `state: offline`：学校返回的离线登记设备；不是账号所有历史设备清单。
- `service`：实际服务；空值表示接口未返回。当前出口可以在账号与 IP 匹配后补齐。
- `defaultService`：缺省服务设置，与实际登录服务分开，不能代替 `service`。
- `serviceMatch`：`match` 已核实匹配、`mismatch` 已核实不匹配、`unknown` 在线但实际服务未知、`offline` 离线。
- `--service` 只核对，不登录或切换运营商，不隐藏未知设备。
- 原 `account-status --json` 的 `summary.devices` 仍保留在线设备列表，现明确增加 `online/state/stateSource`；
  `summary.offlineDevices` 单独列出离线登记记录。原始 `devices.data.onlineDevices` 也已经表示在线。

若要核实未知设备现在是否使用移动，在该设备本机执行 `python netlogin.py current-status --json`，
核对 `summary.userId`、`summary.userIp`、`summary.online`、`summary.service`。
集中查询接口未返回实时服务时，不能从 IP、缺省服务或旧登录记录推断。
页面的设备名称可编辑，但不一定是主机名；用 MAC/IP 与机器实际网卡对应识别设备。

### 4.2 历史登录信息

```powershell
python netlogin.py account-history "学号" --page 1 --page-size 100 --json
python netlogin.py account-history "学号" --start "2026-09-01 00:00:00" --end "2026-09-18 23:59:59"
python netlogin.py account-history "学号" --page-size 100 --ip "10.0.0.2"
```

时间按北京时间输入。`--start` / `--end` 对应学校页面的登录时间与登出时间条件；
同时设置时范围不能超过 92 天。`--page-size` 为 1–200，默认 20；按 `total` 翻页。
`--ip` 只筛选本页，不能将未在本页出现解释为没有历史。

历史记录有 `service`、`loginTime`、`logoutTime`、`terminateCause`。
有登出时间的记录标为 `recordState: completed`；没有登出时间也不会直接认定当前在线。
历史查询不写入当前设备的实际服务字段，避免把上次校园网登录误认为现在仍是校园网。

### 4.3 无感认证、下线和批量操作

| 页面操作 | 含义 | 命令 |
| --- | --- | --- |
| 开启无感 | 登记当前在线设备 MAC，后续接入在学校规则允许时自动认证 | `nosense-enable` |
| 注册无感 | 手工输入 MAC 与有效期，不要求该设备已经出现在在线列表 | `nosense-register` |
| 关闭无感 | 解除无感登记，停止依赖这项登记自动认证；不等于注销当前会话 | `nosense-disable` |
| 下线 | 结束所选在线会话；不删除无感绑定，因此设备可能再次自动上线 | `device-offline` |
| 批量操作 | 页面提供全部下线、全部解绑无感，命令也可重复 UUID 精确选择多台 | 对应命令的 `--all` 或多个 `--uuid` |

开启无感与注册无感使用同一注册接口；前者只传在线设备 MAC，后者可传学校允许的有效期。
自动认证依赖学校策略、有效期与网卡 MAC；Wi-Fi 随机 MAC 改变后可能不再匹配原登记。
无感开关本身不保证指定运营商，仍需查询实际会话核验。

```powershell
python netlogin.py nosense-config "学号"
python netlogin.py nosense-enable "学号" --mac "02-00-00-00-00-01" --dry-run
python netlogin.py nosense-register "学号" --mac "02-00-00-00-00-01" --expire 1 --dry-run
python netlogin.py device-offline "学号" --uuid "在线UUID1" --uuid "在线UUID2" --dry-run
python netlogin.py nosense-disable "学号" --uuid "无感UUID" --dry-run
python netlogin.py device-offline "学号" --all --dry-run
python netlogin.py nosense-disable "学号" --all --dry-run
```

以上为预览示例；核对目标后去掉 `--dry-run` 才发送修改请求。MAC/UUID 均为占位示例。
下线用 `onlineUserUuid`，关闭无感用 `noSenseUuid`，从 `account-devices --json` 获取。
本账号列表中已消失的 UUID 会被拒绝，不会悄悄扩大为全部设备。
`nosense-disable --all` 包含所有已绑定的在线和离线登记设备，比页面 `mode=online` 下的批量解绑范围更广。
`nosense-register` 省略 `--expire` 使用学校配置的期限；每日到期/长期配置不能手工覆盖数值。

退出码：查询/操作成功或预览成功为 0，接口/执行失败为 1，命令行参数解析错误为 2。
修改类操作成功表示学校接口接受请求，随后用 `account-devices` 复查；不自动重试修改请求。
2026-09-18 已实测读取设备、历史、有效期及操作预览；未实际下线设备或修改无感登记。

## 5. JSON 标准输入

### 登录并确保指定服务

将 JSON 通过标准输入传给 `login-stdin`。PowerShell 示例：

```powershell
@'
{"username":"学号","password":"密码","service":"1"}
'@ | python netlogin.py login-stdin
```

`service` 必须为 `0`、`1`、`2` 或 `3`，不能省略；字符串或整数均可。
标准输入可避免把密码放进 Python 子进程的命令行；手工输入的终端内容仍可能被终端历史保存。

### 查询设备

```powershell
@'
{"username":"学号","password":"密码"}
'@ | python netlogin.py account-status-stdin
```

### 下线指定设备

先从查询结果获得真实 `onlineUserUuid`，再使用：

```powershell
@'
{"username":"学号","password":"密码","onlineUserUuids":["查询返回的设备标识"]}
'@ | python netlogin.py account-offline-devices-stdin
```

当前版本中，`onlineUserUuids` 为 `null`、空数组 `[]` 或不提供该字段时，
都会请求下线该账号的全部在线设备。只踢指定设备时必须传入非空的真实标识列表。
该命令下线的是账号设备，不仅是执行命令的本机。

## 6. 校园网卡与 Wi-Fi 配置

默认会主动扫描，并自动选择校园直连检测通过的物理网卡；多个出口通过时优先有线。
需要调整时，首次复制模板：

```powershell
Copy-Item -LiteralPath .\campus_network.example.json -Destination .\campus_network.json
```

已有配置时直接编辑。默认内容：

```json
{
  "scan_wifi": true,
  "preferred_ssid": "iYanDa",
  "interface": "auto",
  "timeout": 4
}
```

| 字段 | 作用 |
| --- | --- |
| `scan_wifi` | 是否主动扫描；false 仍会检查已连接的无线和有线网卡 |
| `preferred_ssid` | 标识希望发现的 Wi-Fi，默认 iYanDa；不据此自动切换 Wi-Fi |
| `interface` | auto 自动检测；或填写 WLAN、以太网 2 等准确网卡名称 |
| `timeout` | 单次网络请求超时，1–10 秒；整个检测包含多次请求 |

还可通过环境变量选择配置文件：

```powershell
$env:YSUNETLOGIN_NETWORK_CONFIG = 'D:\Netlogin\campus_network.json'
python netlogin.py campus-check --json
```

Windows 网卡绑定同时约束 DNS 与认证请求，不修改系统代理、DNS 或路由配置。
仅能在有校园网络访问条件时工作；校园会话可建立，不保证任意公网代理节点都可达。
Linux / OpenWrt 保留原有认证路径，不使用 Windows 网卡扫描与绑定功能。

## 7. 退出码

- 位置参数登录、`login-stdin`：成功 0，失败 1；`login-stdin` 输入错误为 2。
- `account-status`：查询成功 0，查询失败 1，参数错误 2。
- `account-status-stdin` / `account-offline-devices-stdin`：成功 0，失败 1，输入或执行异常 2。
- `campus-check` / `query-machine-status`：检查通过 0，否则 1。
- `wifi-scan`：所有接口确认扫描完成为 0，否则 1；超时结果可能是缓存。
- `current-status`：返回 0 表示命令执行结束；是否在线必须看状态或 JSON，不以退出码判断。
- `logout`：当前版本结束时返回 0，必须检查输出的成功或错误消息。

完整安装与验证范围见 [校园网直连配置说明](CAMPUS_DIRECT_SETUP.md)。

## 8. 校园网 0 下使用代理

`netlogin.py` 的运营商参数只选择校园认证服务，不控制 Clash 的节点或规则。
校园网 0 可用的 TCP/443 节点，以及“经香江-F 连接射手座”的实测结果、持久配置方法，
见 [校园网与代理诊断](docs/CAMPUS_PROXY_DIAGNOSIS.md)。
